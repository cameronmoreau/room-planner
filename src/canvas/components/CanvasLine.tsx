import React, { useContext } from "react";
import { CanvasContext } from "./CanvasContext";

export const CanvasLine = (props: any) => {
  const canvas = useContext(CanvasContext);

  // useEffect(() => {
  //   drawLine(canvas, props);
  // }, [canvas]);

  return null;
};
